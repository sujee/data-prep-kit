###
# Usage:
# envsubst < tokenization2arrow-rayjob.yaml | kubectl apply -f -
###
apiVersion: ray.io/v1alpha1
kind: RayJob
metadata:
  name: rayjob-tkn2a # Name of your RayCluster
spec:
  rayClusterSpec:
    rayVersion: '2.36.1' # Specify the Ray version
    headGroupSpec:
      # Service type for the head node, can be ClusterIP or NodePort
      serviceType: ClusterIP
      # Ray container image for the head node
      rayStartParams:
        dashboard-host: '0.0.0.0'
      template:
        spec:
          containers:
            - name: ray-head
              env:
                - name: DPK_LOG_LEVEL 
                  value: DEBUG
                - name: S3_ACCESS_KEY
                  valueFrom:
                    secretKeyRef:
                      name: s3-secret
                      key: S3_ACCESS_KEY
                - name: S3_SECRET_KEY
                  valueFrom:
                    secretKeyRef:
                      name: s3-secret
                      key: S3_SECRET_KEY
                - name: S3_ENDPOINT
                  valueFrom:
                    secretKeyRef:
                      name: s3-secret
                      key: S3_ENDPOINT
                - name: WORKER_CPU_REQUEST
                  valueFrom:
                    resourceFieldRef:
                      resource: requests.cpu
              image: quay.io/dataprep1/data-prep-kit/tokenization2arrow-ray:1.1.4.dev0
              imagePullPolicy: Always
              ports:
                - containerPort: 6379 # Ray port
                  name: ray-client
                - containerPort: 8265 # Ray Dashboard port
                  name: dashboard
              resources:
                requests:
                  cpu: 8
                  memory: 16Gi
                limits:
                  cpu: 8
                  memory: 16Gi
    workerGroupSpecs:
      - groupName: small-group
        replicas: 5 # Initial number of worker nodes
        minReplicas: 5
        maxReplicas: 5
        rayStartParams: {}
        template:
          spec:
            containers:
              - name: ray-worker
                env:
                  - name: DPK_LOG_LEVEL 
                    value: DEBUG
                  - name: S3_ACCESS_KEY
                    valueFrom:
                      secretKeyRef:
                        name: s3-secret
                        key: S3_ACCESS_KEY
                  - name: S3_SECRET_KEY
                    valueFrom:
                      secretKeyRef:
                        name: s3-secret
                        key: S3_SECRET_KEY
                  - name: S3_ENDPOINT
                    valueFrom:
                      secretKeyRef:
                        name: s3-secret
                        key: S3_ENDPOINT
                image: quay.io/dataprep1/data-prep-kit/tokenization2arrow-ray:1.1.4.dev0
                imagePullPolicy: Always
                resources:
                  requests:
                    cpu: 1
                    memory: 16Gi
                  limits:
                    cpu: 1
                    memory: 16Gi
  # Job details
  # The command to execute the Python script
  entrypoint: >
    python -m dpk_tokenization2arrow.ray.runtime 
    --tkn_tokenizer $TEST_TOKENIZER
    --tkn_doc_content_column curr_file_contents
    --tkn_doc_id_column pulls_and_issues_id
    --tkn_chunk_size 20000
    --tkn_tokenizer_args cache_dir=/tmp/tkn
    --run_locally False
    --data_s3_config "{'input_folder': '$TEST_INPUT_FOLDER', 'output_folder': '$TEST_OUTPUT_FOLDER'}"
    --data_files_to_use "[ '.parquet' ]" 
    --data_max_files -1
    --data_num_samples -1
    --data_checkpointing False 
    --runtime_num_workers 5
    --runtime_worker_options "{ 'num_cpus': 0.8, 'max_restarts': 0, 'max_task_retries': 0 }" 

# Does not seem to be working
#  runtimeEnvYAML: |
#    pip:
#      - boto3==1.35.74
#      - data_prep_toolkit>=1.0.1.dev0
#     - data-prep-toolkit @ git+https://github.com/swith005/data-prep-kit-outer.git@model_loader_s3#subdirectory=data-processing-lib/
